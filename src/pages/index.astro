---
// The homepage. It no longer knows about <html>, <head>, or styles —
// the layout handles all of that. This file is now pure content.
import Base from "../layouts/Base.astro";
import Hero from "../components/Hero.astro";
---

<Base title="mhy — home">
  <h1>Marion Hardy</h1>
  <p style="color: var(--fg-alt);">
    Cell biologist with computer-science skills.
  </p>
  <Hero />
  <p>
    A <a href="#" style="color: var(--accent);">styled link</a> in the accent colour.
  </p>
</Base>